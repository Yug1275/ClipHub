{
  "name": "cliphub",
  "version": "1.0.0",
  "type": "module",
  "description": "Universal clipboard and file transfer system",
  "scripts": {
    "start": "cd server && npm start",
    "dev:global": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev:global\"",
    "install:all": "npm install --prefix client && npm install --prefix server && npm install --prefix local-server",
    "install:local": "npm install --prefix client && npm install --prefix local-server"
  },
  "keywords": [
    "clipboard",
    "file-transfer",
    "redis",
    "express"
  ],
  "author": "Yug",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
